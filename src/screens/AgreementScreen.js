import React from "react";
import { Screen, Card, H1, P } from "../ui/Components";

export default function AgreementScreen() {
    return (
        <Screen>
            <Card>
                <H1>Agreement</H1>
                <P>Next: generate PA agreement preview + PDF.</P>
            </Card>
        </Screen>
    );
}
